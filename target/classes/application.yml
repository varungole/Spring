spring:
  datasource:
     url: jdbc:mysql://localhost:3306/Alibou
     username: varun
     password: 
     driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
     hibernate:
       ddl-auto: update
     show-sql: false
     properties:
      hibernate:
       format_sql: true
     database: mysql
     database-platform: org.hibernate.dialect.MySQL8Dialect
  mail:
     host: localhost
     port: 1025
     username: varun
     password: varun
     properties:
       mail:
        smtp:
          trust: "*"
        auth: true
        starttls:
          enable: true
        connectiontimeout: 5000
        timeout: 3000
        writetimeout: 5000  
